name: CD Pipeline

on:
    workflow_run:
        workflows: ["CI_Pipeline"]
        types:
            - completed
        branches:
            - docker

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy on EC2 via SSH
              uses: appleboy/ssh-action@v1.2.0
              with:
                  host: ${{ secrets.EC2_HOST }}
                  username: ${{ secrets.EC2_USER }}
                  key: ${{ secrets.EC2_SSH_KEY }}
                  script: |
                      cd /home/ec2-user/extension-requests-app
                      git fetch --all
                      git checkout docker
                      git pull origin docker
                      sudo docker compose -f docker-compose.prod.yml pull
                      sudo docker compose -f docker-compose.prod.yml up -d
                      sudo docker compose -f docker-compose.prod.yml ps
        # steps:
        #     - name: Checkout
        #       uses: actions/checkout@v6

        #     - name: Set deployed image tag from CI run
        #       run: echo "BACKEND_IMAGE=${{ vars.DOCKER_USERNAME }}/extension-requests-app-backend:${{ github.event.workflow_run.head_sha }}" >> $GITHUB_ENV

        #     - name: Pull docker image from docker hub
        #       #   run: sudo docker pull ${{ vars.DOCKER_USERNAME }}/cicd-workflows-image:latest # docker pull chriscardenas/my-image:latest
        #       run: sudo docker compose -f docker-compose.prod.yml pull

        #     - name: NO Build and run docker container
        #       run: sudo docker compose -f docker-compose.prod.yml up -d

        #     - name: SHow running containers
        #       run: sudo docker compose -f docker-compose.prod.yml ps
